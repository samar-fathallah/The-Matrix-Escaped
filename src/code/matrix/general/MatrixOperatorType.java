package code.matrix.general;

import code.generalSearchProblem.general.OperatorType;

public enum MatrixOperatorType implements OperatorType {
    UP, DOWN, LEFT, RIGHT, CARRY, DROP, TAKEPILL, FLY, KILL
}
